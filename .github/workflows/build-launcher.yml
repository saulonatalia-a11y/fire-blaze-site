name: Build FIRE BLAZE Launcher (desativado)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build automático desativado. Launcher oficial mantido na v1.2.7."
